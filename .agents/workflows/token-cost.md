# Workflow: token-cost

This workflow runs the token cost calculator script to summarize and analyze API token usage and conversation logs in the workspace.

## Execution Steps

1. Run the cost script:
   ```bash
   python .agents/skills/token-cost/scripts/calculate_cost.py
   ```
2. Present the cost data and transcript statistics to the user.
